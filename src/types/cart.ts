export interface CartItemType {
  productId: string;
  sku: string;
  name: string;
  color: string;
  size: string;
  price: number;
  discountPrice: number;
  calculatedPrice: number;
  image: string;
  quantity: number;
}
